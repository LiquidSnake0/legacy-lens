# Build output
bin/
obj/
[Dd]ebug/
[Rr]elease/
web/node_modules/
web/dist/
web/.angular/

# The index, and any repository mounted for indexing.
# Neither belongs in this repository, one is generated, the other is
# somebody else's source code.
*.db
*.db-shm
*.db-wal
repos/*
!repos/.gitkeep

# Local configuration
.env

# Editors and local tooling
.vs/
.vscode/
.idea/
*.user
# Holds working copies of this repository under .claude/worktrees. Tracking it
# would commit the repository into itself.
.claude/

# OS
.DS_Store
Thumbs.db

# Built by the desktop publish and thrown away after it. The interface's source
# lives in web/; this is where its output is put so one executable can carry it.
src/LegacyLens.Api/wwwroot/

# La version en un fichier : 57 Mo par cible, reconstruits par ./desktop.sh.
build/
